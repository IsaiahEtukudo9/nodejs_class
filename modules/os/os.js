const { log } = require('console');
const os = require('os');
const cpu = os.cpus();
console.log(cpu);

// const osTypes = os.type();
// console.log(osTypes);

// const release = os.release()
// console.log(release);

// const totelMem = os.totalmem()
// console.log(totelMem);
